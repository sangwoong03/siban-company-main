import React from "react";
import styled from "styled-components";

const HomeSiban = styled.section`
	padding: 100px 0;
	background: #000000;
	color: #ffffff;
	text-align: center;
`;

function Siban() {
	return <HomeSiban> 시반 페이지입니다.</HomeSiban>;
}

export default Siban;
